# Mode-specific env for Vitest. Vite loads `.env.[mode]` with higher priority
# than `.env.local`, so these empty values win over a developer's real secrets
# and the suite always runs in demo mode (no Clerk, no network calls).
VITE_CLERK_PUBLISHABLE_KEY=
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
